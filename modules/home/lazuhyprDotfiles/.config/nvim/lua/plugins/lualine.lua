return {
  "nvim-lualine/lualine.nvim",
  opts = {
    options = {
      theme = "tokyonight",
    },
    -- update this to use a function for `opts` and only modify specific values, to avoid overriding some good defaults (ex. filename bolded).
    -- sections = {
    --   lualine_c = { { "filename", path = 1 } },
    -- },
  },
}
